{
  "name": "dify-saas-platform",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "backup:sqlite": "bash scripts/backup-sqlite.sh",
    "restore:sqlite": "bash scripts/restore-sqlite.sh"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
